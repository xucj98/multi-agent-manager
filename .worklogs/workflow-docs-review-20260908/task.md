# 协作入口文字复核

负责人：Chandrasekhar（gpt-5.6-terra max）。此前已作为首次接触项目的空白 agent 完成初审，本次复核同一任务的新要求。

只读审阅原管理仓库 AGENTS.md、README.md 与四库原仓库 AGENTS.md。检查简洁、清晰、歧义和冲突，不调查算法代码、不安装环境。workspace 不需要建立；报告明确标为无。

当前要求：任务文件先于工作，追加要求先写入文件；每个 agent 有结果简报，含完成情况、workspace、各worktree commit（无worktree则注明）；review有自己的任务与报告；归档保留任务资料和状态，只移除workspace，smoke与临时文件由执行者处理。各库日志入口应指向管理库的任务目录。

将完成情况、所读文件和代码commit、必要修正建议写入本目录 report.md，Manager 亲自修改并提交。只报告影响执行的歧义，不为字词偏好反复改动。
