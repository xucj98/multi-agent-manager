# Correct optional wait ownership semantics

User reconfirmed the already agreed task-level rule: an inactive executor with running-only jobs is not Manager pending work; stopped unarchived jobs need executor follow-up; an inactive task with no unarchived jobs needs Manager acceptance/review/requirements. Restored optional wait incorrectly preserved the old blanket inactive-task behavior. Real reproduction: Manager mam wait immediately reports Wash19e98b62 agent_completed while all four job records are running.

Read AGENTS/development docs, create registered MAM worktree from current main21e27c7. Fix this narrow regression in wait_runtime and related existing tests/design text. Inspect BOTH initial snapshot and turn/completed event fast-path; both must re-evaluate current task/job ownership instead of unconditional agent_completed. Do not change one-hour timeout, message/stop wake, unknown-state handling, task review routing, process identity or proactive scheduler routing. No new public flags/APIs.

Required regression cases: idle executor running-only jobs remains in wait until real event/cancel/message; active executor ends with running jobs also continues waiting; mixed running/stopped retains the stopped-job handling (not task_completed); no jobs/all archived idle owner produces Manager follow-up; active review suppresses source task-ready but never stopped job; live job transition stops triggers current expected actionable result with IDs/note. Keep current wait stopped-job notification as handoff to executor, not a claim task finished. With proactive daemon enabled the executor can be awakened by daemon; do not start a second parallel routing implementation within wait. Preserve no probes for an active owner and don't hide unknown observations as running.

Run meaningful CPU tests, commit and publish concise report, clean own transient artifacts, end turn. No global install, daemon/App Server restart, new test threads, live model turns or GPU operations. We are simultaneously diagnosing Windows subagent visibility and must not pollute that baseline with another install liveprobe. Manager handles independent review and bounded installation/verification without creating new sessions. Preserve .local/ui-probe and shell tracing unchanged.
