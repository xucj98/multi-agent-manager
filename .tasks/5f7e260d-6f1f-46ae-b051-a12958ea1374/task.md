# 统一本机robot-bridge环境与worktree输出入口
# Normalize local robot-bridge environment and worktree result ownership

User approved: stable /mnt/public/xcj/Projects/robot-bridge/.venv should use the same shared CPython3.11 as standard worktrees; robot-bridge/eval_result must no longer be created or linked by worktree creation. These are internal setup behaviors; agents should not need new options/instructions. Model gpt-5.6-terra/max. Your old cleanup task8f55159f is archived.

Create independent robot-bridge worktree at ae8829658644450181e6201bdef16fea2bf4daef via mam workspace add, then read AGENTS and appropriate environment guide/conventions. Stable root has unrelated untracked docs/design/low-dimensional-memory-design-space.zh-CN.md; preserve it. Standard local wrapper is ignored robot-bridge/.local/create_worktree.sh, accepts BASE_COMMIT NEW_BRANCH WORKSPACE_ROOT, shared Python /mnt/public/xcj/cache/shared-python/cpython-3.11.14-linux-x86_64-gnu/bin/python3.11, uv cache /mnt/public/xcj/cache/uv hardlink. Base managed installer currently declares ROBOT_BRIDGE_SHARED_ENTRIES=(logs eval_result). C already suppresses eval_result via its local adaptation. Do not touch C or active eval/training trees.

1. Prepare and CPU-validate minimal creation-script fix in own tree so future local worktree creation never creates stable bridge/eval_result or a worktree link to it. Keep existing three-argument local wrapper usage and useful historical base support; no agent-visible flag, new env manager or provenance schema. Report exact intended ignored-wrapper update; authorized to apply that narrow local entry change after validation if it is self-contained, otherwise Manager integrates reviewed tracked script first. Existing result content is not authorized for deletion; only remove an empty obsolete result directory/link if positively confirmed harmless. Keep RMBench output ownership.
2. Verify no active local/wuwen-1 process directly depends on stable bridge .venv, without printing unrelated process environments/commands. If in use, report exact dependency rather than stopping it. Rebuild stable .venv with shared3.11 and matching frozen dependencies/dev extras/cache mode; do not merely repoint python inside3.13 site-packages or link whole venv to a disposable worktree. Prepare/verify rollback before replacement and remove own obsolete backup after success. Keep scripts/shebangs/editable source paths valid at final stable location. User explicitly authorized this stable ignored-environment operation, no global code reset. Do not modify lock solely to install, upgrade packages, use GPU or change robot/PM services.
3. Validate stable Python version/realpath, dependency consistency, import/CPU basic check and worktree creation behavior (appropriate existing checks; no full GPU eval). Record real timing if a fresh create is needed, and clean all owned temporary artifacts. Do not create extra unregistered workspaces. If source code changes, commit only owned tracked changes; Manager reviews/integrates. No GitHub push yourself.

Do not add implementation details about symlink/uv/cache plumbing to user guides. If existing guide needs correction, keep it to how to create/run/find results; prefer giving Manager a concise proposed wording. Report final environment path, code commits, ignored-wrapper changes, validation, cleanup and blockers if any in .tasks/TASK-ID/report.md; publish then end turn. Manager archives workspace after acceptance.
# Explicit final cleanup clarification from user

User reiterated robot-bridge/eval_result is unnecessary and asked whether it had already been removed. Manager confirmed the stable directory was empty and removed it with rmdir during this task. If legacy worktree creation temporarily recreated it, remove the empty stable directory again at final cleanup. Do not preserve an empty stable eval_result solely because old worktrees have unused links to it. Leave other active worktree links untouched; no result files may be removed if contents unexpectedly appear. Final stable bridge/eval_result should be absent and future local create should not recreate it. This follows user's explicit removal decision, not a new guide requirement.
# Final documentation and required repository checks

Manager verified stable Python3.11.14 and absent eval_result; code1e5f3ac is the correct one-line result-entry change. Before merge, simplify docs/worktree_env/README.zh-CN.md as user explicitly requested: usage only. Keep the three-argument creation command, concise meanings/output path, how to run installed .venv and CPU check, and link to existing deployment guide. Remove implementation paragraphs/table about ignored/tracked/symlink checks, stable-source extraction and negative descriptions of which directories are not created. Do not document the invisible no-eval-result behavior as an agent requirement. Maintainer details remain in script --help/source.

Repository conventions section7 requires tests green before merge. Existing environment smoke alone is not the full CPU suite; run the normal pytest tests/ suite in your already-created independent worktree, without GPU/real robot. Report concrete failures if any instead of treating unrelated failures as pass. No new redundant tests are requested. Commit docs refinement separately, publish final test evidence, clean generated artifacts, then end turn. Avoid repeating environment rebuild or process scans; that operation is already completed and verified.
# Close the pre-existing CPU test fixture failure

Manager accepts guide8b1ba04 as concise usage. The only full-suite failure is a test fixture that bypasses RMBenchSimulationController.__init__ but does not supply its ordinary default _trace_enabled=False. Verify baseline ae882965 reproduces this failure without rebuilding environments; inspect actual constructor/fixture to confirm no real behavior issue. Authorize a separate minimal test-only fix that initializes the bypassed default state correctly, preserving the image/scheduler assertions. Do not weaken production controller validation or change runtime behavior just to pass this test. Run the targeted case then normal full CPU suite to satisfy repository merge requirement; publish exact separate test commit, results and cleanup. No environment rebuild/process scan/GPU/robot activity is needed.
