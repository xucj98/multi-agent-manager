# 清理已完成BF16验证的临时模型

用户最新要求：不要迁移user_checkpoints，已完成实验的临时副本及时清理。唯一目标是/mnt/public/xcj/Projects/openpi/user_checkpoints/precision_validation/rearrange_full_key_state_30k_bf16/30000，约4.9GB。验证已完成100rollout92/100，源FP32在RMBench/policy/pi05/checkpoints/pi05_full_key_state/shared_memory_full_key_state_seed0/30000，正式结果RMBench/eval_result/memory_chunk_20260910/precision_rearrange_full_row30_bf16_100ep_seed0。原模型与正式结果必须保留。
先读MAM入口及两库AGENTS。仅清理该已知副本；核对源FP32和正式结果/导出metadata存在、无登记活跃job引用目标。查已登记worktree是否有user_checkpoints专用软链，仅删除确认指向该已废弃目录的链接，不能跟随软链删除外部内容。删除目标后仅用rmdir逐层删除空父目录至user_checkpoints；遇未知其他内容则报告，不扩大删除。无需重读全部权重或重跑GPU验证。
为修改实验说明，用mam workspace add创建RMBench独立worktree，base 6ce7290feca710a9b41adb45f8b647853d6bf389。阅读docs/guidelines约定，在experiments/memory_chunk_20260910/README_precision_validation.zh-CN.md将“BF16 checkpoint保留”改为已验收清理，以及原始FP32与导出留痕位置。不可篡改历史运行command/metadata或历史MAM发布记录。核对OpenPI版本管理脚本是否会重建user_checkpoints；若无此约定不为假想风险改代码。若确实需要改脚本先报告范围，原训练树冻结。
不占GPU、不修改训练树/现有环境、不迁移模型。提交必要文档并发布简报，列删除路径、源/结果留存、变更commit与自身workspace；Manager审阅整合后归档。临时文件自行清理。
