# MAM one-command installation and behavioral compatibility validation

Start from main 351c3a3e6dc807cae280a99d5f3c824f8d5750ff in a MAM-registered worktree. Follow AGENTS/README and publish report/commits. gpt-5.6-terra/max. During development do not restart the live app-server or alter production environment; implement the user-confirmed installer behavior below and test it in isolation. Manager integrates/installs after review. Write scope: multi_agent_manager/wait_compat.py, scripts/install.sh, tests/test_wait_compat.py, docs/install.md. Manager owns README/AGENTS/design. Runtime task owns cli.py, job_runtime.py and other wait modules.

## Goal
User requests one-command MAM install+test and obvious errors ONLY when behavioral compatibility tests fail. Version changes themselves are NOT errors; new versions passing the tests must work. A message must never silently sit for 1h because integration broke. Keep small, no new dependency framework/daemon. Use stdlib + existing pipx/install entry. Inspect existing install docs first. Script must work from explicit checkout and installed CLI. Install failures/tests errors nonzero, clear summaries. The installer may restart app-server ONLY after the user explicitly types yes at its CLI prompt, per the final workflow below.

## Runtime contract (coordinate Manager if changes)
Provide `wait_compat.require_compatible()` no args -> dict with socket_path, log_path and validated fingerprint; raises RuntimeError/actionable explanation on incompatibility. Runtime calls before blocking. Check actual running app-server behavior and connected proxy/runtime where observable, not just `codex --version`. Version is diagnostic information, not a gate or allowlist. A version change may trigger revalidation but must not itself fail require_compatible or block waiting when behavioral checks pass. Avoid a version-certificate approval workflow. No false PASS from merely unit tests or version string. Expose a narrow Python entry for installer self-tests if needed; do not add many user CLI flags.

Current actual server0.154.0, Windows App26.901.51231, official npm codex app-server proxy; app-managed process, NOT standalone daemon-managed. Socket /root/.codex/app-server-control/app-server-control.sock. Managed daemon restart cannot be assumed. Installer must support the existing app-managed npm runtime, with explicit yes confirmation before restart; do not silently convert it to a standalone daemon or assume standalone install exists. Runtime may use trace log /root/.codex/app-server-control/app-server.log, RUST_LOG=off,codex_app_server::message_processor=trace,codex_app_server::app_server_tracing=info LOG_FORMAT=json. Known hook input cannot wake waits: executes after tools return.

Evidence in root .local/hook-probe/: watch_agent.py/agent-events.jsonl prove event subscription; check_mapping_trace.py isolates an ephemeral server and validates trace turn.id without model calls. Server log maps request to turn.id before wait ends. Test whether prerequisites actually work; don't simply trust log path exists. Keep payload/message bodies out of diagnostics.

## Validation scope
Unit tests must prove a changed/new version can PASS, and that broken event delivery, missing trace/capability, disconnected runtime and failed behavioral checks produce clear errors. Server/CLI version differences alone must not fail. Keep version metadata only for troubleshooting. Useful live protocol checks can use an independent temporary stdio server or read-only current control socket; must not start real model work or alter live threads. Full user-message/native-manager-send end-to-end checks require Manager coordination; report what cannot yet be certified instead of marking it passed. Installer should indicate exact needed test/restart action. User seeks one practical installation/test flow, not a large management system.

Run own tests and full suite. Publish small report with evidence and commits; clean temporary schemas, ephemeral processes/files. Do not keep active turn in a minute polling loop. Report blockers/contract questions then end turn so Manager can act.

## User correction (latest, authoritative)
Not “version changes cause error”; only failed tests cause error. Do not introduce version pinning, allowlists, mandatory approval certificates, or a fingerprint mismatch error. Revalidate behavior when appropriate; if tests pass, proceed.

## Final state-based semantics
Runtime now intentionally uses current-state pending-work checks, no completion replay/cursor/ack. Tests should cover inactive unarchived task returns (except delegated source under unarchived review), active owner job responsibility, and repeated return until handled. Only failing BEHAVIOR tests cause compatibility error, never version change. User asks no short polling.

## Final user installation workflow and explicit restart confirmation
User starts from fresh PROJECT_ROOT, creates .mam/env.json, clones multi-agent-manager into it, enters repo, installs pipx with sudo apt install pipx, then runs `bash scripts/install.sh`. This single entry is responsible for installing MAM, modifying .bashrc as needed for event logging (before noninteractive early return), restarting Codex app-server, and running behavioral compatibility tests. Failed tests must return nonzero and clear cause. Versions are diagnostics only. docs/install.md must describe this actual short workflow.
Before ANY stop/kill/restart of existing app-server, prompt in the terminal with exact target PID/executable/socket and ask for `yes` + Enter. Default/no/EOF/noninteractive input must NOT kill; clearly report installation/verification incomplete if restart is needed. Never accept inherited flags as consent. Revalidate PID/start identity after confirmation to avoid killing replaced process. Do not kill all codex processes, the UI, GPU jobs, or unrelated project processes. Maintain existing app-managed npm startup style. Handle installer independent terminal requirement because the connection executing via App may be severed by restart. Do not claim changing env alone changes an already-running server.
Make .bashrc edits minimal/idempotent with marked block and preserve unrelated content/permissions; no repeated export accumulation. Integrate/remove only recognized previous test lines where exact match. Keep a reversible backup and give rollback instructions. PATH for pipx command should work in fresh shell and current installer diagnostics. If auto restart for current deployment cannot be implemented reliably, report concrete reason to Manager rather than implementing a misleading PASS or asking user to perform multiple hidden steps.
The yes prompt is user-requested installer behavior, not a new agent permission gate. Manager will coordinate any real destructive-to-connection live test; agent must not restart current production app-server while implementing.

## Integration cleanup finding
Combined suite emitted ResourceWarning at wait_compat.py:412 (_behavior_probe): unclosed subprocess stdin/stdout files. Close all streams in success/error/timeout cleanup; publish follow-up commit and test. Runtime owner separately fixes compatibility mock test. No production restart.

## Manager-adjudicated independent review fixes
Review task 8d648d66 report findings 1 and3 accepted as real installer issues (severity labels not material). Fix shell exit status loss in runtime_logging_ready AND caller inversion; test missing env vs unreadable PID separately. Current self-tests passing is insufficient.
Restart must deterministically restore the existing app-managed npm listener with required environment after exact yes; relying solely on Windows App to reconnect in20s does not fulfill one-command installation. Within installer, capture verified current executable/argv/cwd and minimal required launch environment before TERM; after confirmation revalidate process identity and use the original supported startup command/style with trace overrides, under appropriate existing startup locking/race checks. Do not replace npm-managed runtime with standalone daemon or install a new background supervisor. Verify replacement socket/log/env after startup. If App concurrently replaces target, do not kill replacement; validate it or fail clearly. Closed Windows App should not make installer incapable of recreating listener. If there is an unavoidable blocker to deterministic same-style startup, report it before finalizing a partial script. No actual production restart during agent development; use isolated process harness to validate command/env/yes/EOF/race behavior.
