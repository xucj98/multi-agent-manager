task_revision: f4dfdfc35aa5ea86e2696f9a57d4623d0c5e5db9

完成与未完成：已以首次接触本研究的读者身份完成主框架及任务限定五份历史证据的只读审阅；未修改主文档、未读取旧评语/其他任务/聊天，未创建代码 worktree、环境或启动实验。

workspace、各库交付 commit：归属 workspace 为 `/mnt/public/xcj/Projects/workspace/a9b51a22-d6e9-4940-9f71-1c6335ce5739`；本任务无业务库交付 commit。论文仓库 HEAD 为 `ee3f411dfa94d1fce4790d9c6b580ce16e1dc019`，且该未提交讨论稿不在该 HEAD 中。

验证结果与成果位置：已核对主文件 `/root/Documents/task-state-vla-paper/docs/MEMORY_DESIGN_FRAMEWORK.zh-CN.md` 的 SHA256 为 `39d9a9a222dd2022c7c71ae97105c4b767ffc6bbc3bbc161e23e446b3ca90d14`；历史数值、full/serial 时序映射及其证据边界与限定的 history_audit README、run_index、shared、rearrange token、early-state 材料一致。成果即本报告。

我理解的核心研究问题：在 action-chunk VLA 中，给定任务记忆的语义，输入、状态监督、动作条件化和执行后反馈应怎样组合，才能产生可解释、可比较的闭环收益，以及这些选择的效果受哪些执行条件影响。

审阅结论：接受局部修订。S/P/E 分层、h/d/Δ 的时间约定和对历史 full/serial 的归位是自洽的；P1、P2、P3也分别限定为设计选择的端到端效果，而没有把现有混杂结果冒充机制结论。历史表保留了单一训练 seed、eval commit 不齐和 Oracle 替换位置等限制，足以指导优先级。以下三点会影响候选对照的可执行性或篇幅。

1. **必须修正｜P2 的末尾样本与损失总体尚未定义。** 位置：§5.3，尤其是“固定 S、I、C、A、U”和两种 H 行目标（约 183–196 行），对应 §2.1 的 terminal 定义与 §2.2 的有效位置权重。重复终点需要 `phase(t+30)`，逐帧版本需要直到 `t+50` 的参考；接近 episode 结尾时，二者若采用不同的丢样、重复末值或 mask，比较会同时改变训练样本总体/权重，不能只归为 T 的时间映射。最小修改是在 P2 表中写明终止后的状态与 action padding、有效样本和位置 mask，并明确两格使用相同的样本集合、动作目标及归一化。

2. **必须修正｜P3 把语义 `NA` 与无信息对照混用。** 位置：§5.4（约 200–204 行）相对 §2.1 的 `domain`/`not-applicable` 约定；限定证据中该按钮字段的 `NA` 只在 P0/P2 阶段表示不适用，P1 使用 unconfirmed/confirmed。若整段实际可按压阶段也写成 `NA`，它是人为掩码而非该字段的语义真值；新读者无法据此确定三类槽位、embedding、损失、解码和回灌是否仍完全同形。这样会削弱“按钮语义输入与监督”的归因。最小修改是把它明确命名为 P 层的 `masked/no-info` 编码（或明确 NA 的消歧映射），逐项固定类别数、参数形状、loss 和 U 的读写规则。

3. **可选精简｜P3 的确认性重复条件可与 P2 对齐，并压缩重复的方法论说明。** 位置：§5.4（约 198–208 行）及 §2.4、§3（86 行）、§5.2（177 行）、§5.3（190–196 行）。P3 将报告差中之差，却只说“需要新的训练”，没有像 P2 一样给出训练 seed 数和四格共用 evaluator/配对初始条件；若它是确认性比较，应补这一句，若仅探索则明示。关于“设计效应不自动识别机制”的正确限定已在多处重复，可保留 §2.4 的总规则和各实验唯一的具体混杂，从而让首次阅读者更快看清 P1–P3 各自回答的问题。
