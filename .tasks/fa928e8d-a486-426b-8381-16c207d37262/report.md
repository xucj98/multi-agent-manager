# battery S seed0：候选已就绪，50-step gate 因实际 GPU 容量未通过

本任务从冻结 OpenPI `34002dce65962734c59725a0f6d982ae2c438a2d` 建立独立 worktree：`/mnt/public/xcj/Projects/workspace/fa928e8d-a486-426b-8381-16c207d37262/openpi`。该 commit 已含 `pi05_rmbench_battery_try_serial_lag30` 和 `battery_try_serial_lag30.yaml`；没有必要的源码变更，worktree 仍 clean，故候选源码 commit 就是该冻结 commit。

候选与已验收 battery J 共用 `battery_try_demo_clean_state_shared_memory` 的 50 条 `demo_clean_state` semantic sidecar、`rmbench_battery_try_robot` 14D norm，以及同一 `phase` 定义：`place_initial_00 / try_10 / try_11 / try_01`。绑定明确为 `series.phase=current_truth`、`availability.phase=availability_at_row`，robot target 是 `action_at_row`。S 的唯一区别是 `serial_token`：固定 `previous=30`，train condition 为 reference、infer condition 为 selected，query 监督及 `query_selected` feedback；seed0、bs32、H50/K30、pi05_base 独立初始化、BF16 model-only、20k/save20k 均已由 config 直接核对。phase-only 不表达完整 attempted-set history，未修改这一限制。

CPU gate 已通过：worktree environment smoke；battery semantic template test；serial checkpoint metadata roundtrip test；battery adapter 的 6 项测试。实际 `seed=0`、2-worker batch 从默认 `/root/.cache -> /mnt/public/xcj/cache` 读取，`HF_LEROBOT_HOME` unset，得到有限的 action `[32,50,32]` 和 serial input/target/mask `[32,1]`。机器可读输出 [battery_s_real_batch_cpu.json](/mnt/public/xcj/Projects/multi-agent-manager/.tasks/fa928e8d-a486-426b-8381-16c207d37262/evidence/battery_s_real_batch_cpu.json) 的 SHA-256 为 `71a0472dfacbed336430cd3f10aa91c579f159652093a7c318d2615ff8396024`。

已创建保持同一正式合同的 [50-step launcher](/mnt/public/xcj/Projects/multi-agent-manager/.tasks/fa928e8d-a486-426b-8381-16c207d37262/evidence/launch_battery_s_smoke.sh)、checkpoint-only CPU/GPU validator 和 [20k candidate launcher](/mnt/public/xcj/Projects/multi-agent-manager/.tasks/fa928e8d-a486-426b-8381-16c207d37262/evidence/launch_battery_s_formal20k_candidate.sh)。20k launcher 只有在 `MAM_FORMAL_20K_APPROVED=fa928e8d-a486-426b-8381-16c207d37262` 时才会启动；未经该标记已测试为拒绝，正式输出根仍不存在，未启动 20k。

GPU2 的一次授权 50-step 尝试使用原合同的 `XLA_PYTHON_CLIENT_MEM_FRACTION=0.90` 和 `XLA_FLAGS=--xla_gpu_enable_command_buffer=`。启动前该卡已有约 14.7GB 隔离占用；真实 batch 与 pi05_base 加载正常，但首个训练 step 还需 19.80GiB，报 `RESOURCE_EXHAUSTED`。训练 PID 已退出、GPU 已归还、没有完成 checkpoint，因此 CPU/GPU checkpoint restore 尚不能执行。原始 [OOM receipt](/mnt/public/xcj/Projects/openpi/checkpoints/battery_s_smoke_34002dce_nocmdbuf_20260914T110754Z/validation/battery_s_smoke_attempt1_oom_receipt.json) SHA-256 为 `6b028e1acbafeecab52b41472603d2aaaf9d2a18e37746ac2763c5b56ef9e9d1`，训练日志 SHA-256 为 `96431ef5d9d0e210144239ff8cb3fc0549f34c11a8429c586d698f80b4805196`。

当前结论：配置、数据和 executable candidate 已就绪，但 **尚不可准入 formal 20k**。需要一张实际可提供约 75GB 空闲显存的单卡，以不改动合同的方式重跑 50-step 保存、CPU 全参恢复和 GPU checkpoint-only policy 恢复；三项通过后再由 Manager 决定正式准入。完整当前状态见 [receipt.json](/mnt/public/xcj/Projects/multi-agent-manager/.tasks/fa928e8d-a486-426b-8381-16c207d37262/receipt.json)，SHA-256 `1104d3b5487f756572c5706ce04c01002a1ed99477f735c166af2eaf437d77ef`。
