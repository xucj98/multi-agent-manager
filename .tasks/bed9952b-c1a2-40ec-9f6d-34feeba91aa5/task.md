# 空白使用者实操验收

你没有此前对话历史。本次检验的是：新人能否仅依靠文档完成B/C环境准备并开始一个实际短任务。起点为本机MAM库 `.local/README.md`，随后仅沿显式入口阅读相应集群说明、各库AGENTS.md及其引用的操作文档。MAM本身使用入口AGENTS.md/README。不得读源实现、安装脚本内容、其他task历史报告或现有任务私有launcher来猜操作；允许执行文档命令、读帮助/报错、列目录、查询git身份和资源。帮助若补足了文档未说明的必要步骤，须如实记为额外步骤，不能算原文档直接通过。

实际任务：分别在B的wuwen-11和C的wuwen-4090-1，用文档三参数入口创建本TASK-ID独立workspace下的openpi、RMBench、robot-bridge环境。测试输入已给定：OpenPI base=34002dce65962734c59725a0f6d982ae2c438a2d；RMBench base=f401f5279c95451eb424ac98b831bab5552b2120；robot-bridge base=f9626636c4776d8eb15f9c556775cb2d12c000e5。每库branch=task/bed9952b-c1a2-40ec-9f6d-34feeba91aa5。主机/根路径从文档获取，不使用隐藏提示。

创建成功后，按各库文档执行其标准环境smoke/自检，至少每集群实际运行一项文档中的CPU功能检查（不是只运行--help或空队列dryrun），记录具体命令、返回码与结果。此任务验收环境准备和短任务启动，不授权训练、GPU rollout、大模型传输或改动他人环境。若文档只提供GPU功能检查，先列出准确命令与资源要求，报告覆盖缺口，不抢运行中GPU。

可以正常安装文档声明的依赖，使用稳定installer自动步骤；只能写本任务workspace/分支和installer正常管理的cache，不改稳定脚本、共享配置或修复文档。缺base object、稳定入口/环境依赖/路径报错等均应记录是否文档给出恢复办法；无文档支持时停止该分支，不自行补丁，继续另一库/集群可独立工作。预计>30分钟按MAM要求登记实际进程。不得以已有环境存在替代新建验收，也不得声称GPU/训练能力已验证。

每个问题保存：文档路径/具体段落，按说明执行的原命令、stdout/stderr、首次失败原因、继续所需但缺失的信息。给B/C各库矩阵：创建/安装/环境检查/实际CPU任务是否通过；区分文档缺失、实际代码缺陷、外部资源问题。保留最小日志和新环境供Manager裁决，不清理其他任务。发布report，禁止替作者直接修好后宣称初次通过。先回传CODEX_THREAD_ID，等待MAM绑定前仅阅读入口。
