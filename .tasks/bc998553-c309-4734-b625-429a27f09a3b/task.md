# MAM proactive wakeup implementation

## Goal and authority
User approved replacing agent-invoked `mam wait` with a persistent project-local background scheduler. Manager handles architecture/adjudication/integration and README.md/AGENTS.md. You implement runtime and CLI in an isolated MAM worktree from main 330252072cb3c2a834a69a9d63cd1e34627668fa. Read MAM AGENTS.md, current published task, development guide, then use `mam workspace add` and read worktree AGENTS. Work locally, no GPU, robot/PM or production app-server restart. Preserve all unrelated dirty files and existing jobs. Model gpt-5.6-terra/max.

## Required behavior
- Agent has executable work: continue. Nothing executable: normal final/turn end; no wait registration, heartbeats, timer instructions, handoff flags or extra delivery signal. Turn end != task archival. Ordinary command/test waits remain ordinary tools.
- Evaluate each nonarchived TASK independently. Archived jobs never probe/wake. Stopped unarchived jobs take precedence over running jobs and route to that TASK executor; when executor idle, wake it. When executor active, retain the actionable item until it can be delivered or is resolved, do not start a parallel turn or interrupt work.
- If no stopped jobs but some running jobs, simply monitor them; do not wake Manager because owner ended its turn.
- If all jobs archived or no jobs, and executor active, wait. If executor idle, task is Manager work: inspect/accept/archive, create reviewer, or publish new requirements and notify executor.
- Existing `--review` links suppress source-task idle reminders while review task remains unarchived. Review task follows same rules, review archival re-exposes source. Never hide source stopped jobs; cycles/unknown ownership must be diagnosed.
- Durable project-isolated pending events and delivery bookkeeping. Reconcile after startup/disconnection/restart; event when recipient busy, completed turn, service down must not be lost. Batch multiple pending events per recipient. Persist unresolved items; deduplicate accepted wakeup so same unchanged idle state does not continuously start turns. A delivery does not archive tasks/jobs. Explain acknowledgment/retry ambiguity handling explicitly. Recheck condition and recipient immediately before turn/start; cancelled/stale events must not wake. Failed send remains pending, transient errors internally retry with bounded backoff; unknown must never become stopped. Sustained/identity/config errors surfaced in service status/log without model loops. No automatic process termination or task/workspace deletion.
- User and Manager messages work through native Codex; there is no wait to cancel. Preserve user control: do not steer/interrupt an active user turn, restart an explicitly paused/interrupted task in a loop, or overwrite model/effort/cwd. Resume idle existing threads via real App Server API, preserving settings and task workspace context. Native close_agent and turn end differ; handle missing/unloadable thread and capacity errors visibly without unrelated thread creation.
- Remove public mam wait/list/stop and obsolete wait plumbing/tests that no longer serve feature. Internal useful process/agent queries can be reused, no mandatory compatibility alias. Do not leave tracing/hooks solely for wait-message detection if no longer needed. Coordinate shared API/import changes with installer owner via Manager.

## Architecture contract / ownership
Keep standard-library implementation, current filesystem Store and AppServerEventStream primitives. One scheduler per MAM project, identity-safe singleton/start/stop, detached from tool/agent process lifetime. Persist service state below MAM_ROOT/.local (not Git). Minimal administrative `mam service start`, `mam service stop`, `mam service status`; start may accept `--manager AGENT-ID` for explicit setup, never silently select another project/ambiguous manager. Automatically capture Manager from CODEX_THREAD_ID when unbound caller creates/binds task, do not mistake reviewer/executor for Manager; persisted manager allows ordinary shell install. Legacy current Manager ID for migration is 01a081fe-d6c2-74f2-a73d-68584e9d915b. Do not enable production delivery yourself.
Expose importable start_service(config, manager=None), stop_service(config), service_status(config) in multi_agent_manager.wake_runtime for installer (or propose smaller equivalent immediately before divergence). start_service ensures readiness/compatibility, status clearly distinguishes alive/healthy/disabled/error/pending. Installer owns automatic startup/recovery integration and its tests. You own cli.py, job_runtime.py changes if needed, new wake_runtime.py/modules/tests, removal of wait_runtime.py and wait runtime tests, design doc docs/task-management-design.zh-CN.md. Do NOT edit scripts/install.sh, wait_compat.py or its tests, docs/install.md, README.md, AGENTS.md. Inform Manager immediately of proposed API or behavioral changes affecting installer. Installer task e7183b34-6d86-4394-ada8-675f54b56c42.

## Verification / delivery
Meaningful unit and subprocess/fake App Server integration tests: routing matrix incl mixed running+stopped, empty task, active recipient, review suppression/archive, concurrent starts, crash/restart/uncertain RPC delivery, transient SSH failures, event retention while recipient busy, multiple projects, task mutations under Store lock, no unchanged repeated model turns, no global scans per job every second. Work with Manager for real registered test agents end-to-end before production enable; no unregistered test threads. Run full unittest suite. Deliver commits, concise report at shared MAM_ROOT .tasks/TASK-ID/report.md with design decisions and tests/limits. Publish report. Finish turn when coding done; no indefinite wait. Do not install, push or alter production lifecycle. Independently reviewed before merge.

## User clarification: wakeup payload
Reuse the old mam wait result information as concise wakeup prompt content: stopped job => JOB-ID and note plus task context; task/executor ready => AGENT-ID, TASK-ID, task-title. Do not invent long system reminders, repeat AGENTS or task text, or require extra agent wakeup commands. Native user/Manager messages are ordinary Codex input: remove received new message event entirely. No hour timeout or cancelled wait wakeup reason exists after wait removal. Prompt must state factual reason, not infer success or task completion from a stopped process. Multiple items may be batched.

## Manager integration acceptance details
Normal state changes should become a pending item within one configured background probe interval, with observable timestamps/counters for observed/queued/accepted delivery; an unchanged quiet period must produce zero turn/start calls. Do not hydrate/resume every archived or idle historical thread just to monitor it; that may consume native concurrent thread slots. Use metadata queries and subscribe/resume only as necessary, and document capacity failure handling. Integration will prove same existing executor can end its turn with a real registered short-lived job, then receive the job-id/note prompt after it stops, plus a separate Manager-ready path. Installer must not enable delivery to real training tasks before explicit rollout step.

## Shared installer/runtime contract (Manager settled)
The host has no usable systemd bus. Runtime owns identity-safe detached single-project daemon lifecycle (subprocess/session detached, explicit startup readiness and reconnect, durable state); installer calls these APIs, does not implement another supervisor. Expose synchronous start_service(config, manager=None), stop_service(config), service_status(config), returning JSON-serializable mappings and raising RuntimeError (or cli.Error subclass) for failure. Status includes running, healthy, manager, error/pending information as applicable. Internal state under MAM_ROOT/.local/service; let runtime own exact filenames. Installer compatibility module is multi_agent_manager.wake_compat with require_compatible() -> mapping including socket_path, capabilities, diagnostics; raise RuntimeError on failed behavioral probe. This replaces wait_compat (no trace-log or message-interception prerequisite). Runtime uses this check at start/reconnection, not each probe iteration. Installer may start only after checks pass; current production rollout/real E2E activation remains Manager responsibility. Missing manager: preserve record if one exists; otherwise explicit mam service start --manager AGENT-ID, no silent guessing. Installation can succeed for code but must return nonzero if requested automatic service activation cannot establish manager/readiness.

## Fresh-project bootstrap clarification
The user's fresh install flow should not require an agent to exist first. If no Manager is yet recorded and the project has no bound active tasks, installation/start may leave the daemon explicitly in awaiting_manager idle state; it must register the Manager on the first Manager task create/bind and proceed without another install. Do not claim task delivery is active before binding. For an existing project with bound tasks but no determinable Manager, fail visibly and provide mam service start --manager AGENT-ID. No heuristic selection of arbitrary unbound Codex threads. Test this fresh-install -> first-task -> automatic registration transition.

## Manager early code-review corrections
1. Remove LEGACY_MANAGER_ID/hard-coded session-specific migration from shipped library. The ID above is an operational fact for Manager deployment, not a constant to embed. We will explicitly register it once during production rollout. No legacy wait compatibility machinery is necessary for that.
2. Actual App Server returns notLoaded for known completed/unloaded task threads. Treat an existing queryable notLoaded thread as dormant/resumable, distinct from unknown/query failure/deleted/missing. Eligible stopped-job owner should be resumed and rechecked before start; no-job notLoaded executor should expose Manager work. Merely filtering recipient_state == idle will miss real registered owners in this project. Test both metadata notLoaded -> resume idle and deleted/resume failure cases. User interruption/paused state must not be blindly overridden.
3. Current _deliver stores attempting before RPC but _delivery_failure blindly retries uncertain start. Test server accepts turn/start then response/connection is lost, recipient completes turn before next observation, and daemon restart in that window. Reconcile recorded attempt with actual recent thread turn(s)/input before another start; do not claim exactly-once if API cannot prove it. Keep unresolved ambiguity visible and avoid uncontrolled repeated model requests. The original concise prompt can include an internal stable delivery token only if needed for correlation; prefer existing job-id/task-id plus recorded before-turn boundary.
4. Native service must distinguish actual turn end from task completion and metadata activity changes; no resume-all thread hydration while monitoring.

## Manager accepts independent-review blockers; fix before integration
Review report .tasks/3c97bd4f-14e4-42bb-b7ec-ecf7aad0d5ff/report.md independently reproduces three required fixes against bf157209/f114fd6. Manager accepts all three as blockers (not optional suggestions):
1. Interrupted/paused recipient blocked with next_attempt_at=None must become eligible once an explicit new completed/user-resumed turn is observed. Preserve the interruption boundary and reobserve cheaply; do not require unrelated job/task signature change or manual file surgery. Do not start while still interrupted or active.
2. Source metadata unknown is not positive resolution. Preserve existing pending/accepted task_ready across transient source query failure; idle -> unknown -> idle for unchanged task must not create a second Manager turn. Handle immediate pre-send query failure similarly: retain instead of resolving on inability to read. Test true archive/review/agent reassignment still invalidates old events.
3. Preserve a structured distinction between explicit JSON-RPC rejection and transport/response loss in AppServer client (subclass compatible with existing AppServerEventError is sufficient). Known failed turn/start remains retryable after active recipient ends, not uncertain/ambiguous due to an unrelated new turn. Real response-loss still uses safe boundary reconciliation.
Add independent transition regression tests matching review reproductions, run full suite, commit follow-up on your task branch and publish updated concise report. Update affected design semantics. Do not modify installer-owned files. Manager/installer already have bf157209; provide only follow-up commit(s) for cherry-pick. Reviewer will recheck final combined candidate.
