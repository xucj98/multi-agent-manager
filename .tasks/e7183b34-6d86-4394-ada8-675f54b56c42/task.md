# MAM proactive wakeup installer and compatibility

## Goal
User approved replacing mam wait with background proactive wakeups. Implement installation/lifecycle/compatibility part in independent MAM worktree based on main 330252072cb3c2a834a69a9d63cd1e34627668fa. Read MAM AGENTS and development guide, create own worktree with mam workspace add, then read its AGENTS. Model gpt-5.6-terra/max. Manager handles README/AGENTS and deployment; runtime owner task bc998553-c309-4734-b625-429a27f09a3b handles CLI, background runtime and design doc.

## Scope and contract
You own scripts/install.sh, replacement for wait_compat.py and tests/test_wait_compat.py, extra installer/compat modules/tests, docs/install.md. Do NOT change cli.py, job_runtime.py, wait_runtime.py, wake_runtime.py, README/AGENTS or design doc. Runtime owner exposes start_service(config, manager=None), stop_service(config), service_status(config) in wake_runtime. Admin CLI mam service start/stop/status; optional explicit --manager AGENT-ID for setup. Runtime stores unique Manager from CODEX_THREAD_ID at Manager task create/bind or persisted prior setup; no assumption shell installer has CODEX_THREAD_ID. Send required API changes to Manager before divergence.

User-facing install remains initial project .mam/env.json, clone MAM, sudo apt install -y pipx, bash scripts/install.sh. Install/update must install current checkout through pipx, ensure ~/.local/bin PATH, run tests plus behavioral compatibility probes, configure/start single project scheduler independent of shell/app/agent turn. Provide reliable process lifecycle/recovery in this actual cluster (inspect systemd/user availability; no unnecessary new infrastructure); keep project isolation and explicit health/failure diagnostics. Updating must stop/restart only same project scheduler after compatibility passes, never duplicate it; preserve pending events, manager binding and unrelated project/runtime. A failed compatibility test must return nonzero. Version changes alone must not fail. Do not claim E2E success from merely initialize or mocked RPC; separate smoke capability checks from real managed-agent acceptance coordinated by Manager. Investigate smallest safe real behavior probe that install can automate without unregistered model threads or modifying user threads.

With no blocking wait, user/Manager messages need no interception; remove dependency on proxy trace/hook integration if only needed for wait cancellation. Avoid trace RUST_LOG/npm app-server restart machinery that is no longer necessary. Preserve user bashrc lines. If some actual App Server restart is needed, existing user rule stays: explicit interactive yes before precise identity-checked restart, never broad kill. Do not restart live App Server or modify current bashrc/install yourself while developing. Installer must show clear startup/unavailable-manager failures instead of silently claiming wakeups work. Logs and runtime data must not leak environment secrets. Backend callbacks/lifecycle should not consume model requests while no event.

## Delivery
Unit/subprocess tests for install upgrade failure, service singleton, correct interpreter and manager/root context, startup acknowledgment, setup in fresh project, stdout diagnostic nonzero failures, no background shell dependency, restart recovery. Full suite once combined. docs/install.md concise operational instructions; implementation belongs development/design not README. Report exact lifecycle choice and remaining acceptance needs before merge; no production activation/push. Commit, publish concise report .tasks/TASK-ID/report.md in shared MAM root, finish turn. Clean your short smoke artifacts before archive, no GPU runs.

## Shared installer/runtime contract (Manager settled)
The host has no usable systemd bus. Runtime owns identity-safe detached single-project daemon lifecycle (subprocess/session detached, explicit startup readiness and reconnect, durable state); installer calls these APIs, does not implement another supervisor. Expose synchronous start_service(config, manager=None), stop_service(config), service_status(config), returning JSON-serializable mappings and raising RuntimeError (or cli.Error subclass) for failure. Status includes running, healthy, manager, error/pending information as applicable. Internal state under MAM_ROOT/.local/service; let runtime own exact filenames. Installer compatibility module is multi_agent_manager.wake_compat with require_compatible() -> mapping including socket_path, capabilities, diagnostics; raise RuntimeError on failed behavioral probe. This replaces wait_compat (no trace-log or message-interception prerequisite). Runtime uses this check at start/reconnection, not each probe iteration. Installer may start only after checks pass; current production rollout/real E2E activation remains Manager responsibility. Missing manager: preserve record if one exists; otherwise explicit mam service start --manager AGENT-ID, no silent guessing. Installation can succeed for code but must return nonzero if requested automatic service activation cannot establish manager/readiness.

## Fresh-project bootstrap clarification
The user's fresh install flow should not require an agent to exist first. If no Manager is yet recorded and the project has no bound active tasks, installation/start may leave the daemon explicitly in awaiting_manager idle state; it must register the Manager on the first Manager task create/bind and proceed without another install. Do not claim task delivery is active before binding. For an existing project with bound tasks but no determinable Manager, fail visibly and provide mam service start --manager AGENT-ID. No heuristic selection of arbitrary unbound Codex threads. Test this fresh-install -> first-task -> automatic registration transition.

## Manager early installer review corrections
- Preserve persistent PATH setup. Exporting PATH only inside bash scripts/install.sh does not fix the caller/new terminals; user previously hit mam command not found. Maintain an idempotent marked ~/.local/bin PATH entry in .bashrc (before noninteractive return as needed) and login shell path, preserving unrelated lines. Remove only exact installer-owned obsolete trace block safely, with backup; do not require/restart Codex merely to change tracing.
- initialize/initialized alone cannot certify event_stream validated or wake compatibility. Lightweight runtime check must verify concrete read/resume/latest-turn/turn-start API behavior needed by actual implementation, without model requests per reconnect. Do not claim an unobserved notification capability. Installation acceptance additionally needs actual isolated managed-thread delivery test, not a PASS based only on handshake. Coordinate an implementation proposal with Manager if the minimal end-to-end probe requires real short model turns; any test threads/tasks must be registered/cleaned and never mutate real user threads. Automatic installer probe must distinguish its actual capabilities from coordinated production smoke.
- Do not impose MAM_ROOT == code checkout restriction: user separates code main from project state branch; installing from a main worktree of the same Git repository must remain supported while .mam/env.json selects real state root. Validate same git-common-dir if needed, never overwrite CHECKOUT_ROOT with MAM_ROOT.
- Retain useful specific failure reason in install output (bounded/sanitized as needed); do not collapse all service errors into missing-manager diagnosis. No need to hide ordinary implementation error text on speculative secret grounds.

## Manager decision: one-command real delivery acceptance
Your missing-thread RPC capability probe is useful lightweight reconnect validation, but does not satisfy the user's one-command behavioral acceptance by itself. Implement an install-time live delivery probe as a separate module (not run on periodic reconnect): create a disposable isolated MAM fixture project, register its task before creating/binding dedicated probe thread(s), bind executor before first model turn, and use the real App Server and background service to verify existing executor turn end -> real registered short job stops -> scheduler starts a new turn with exact job-id/note. Also verify no-job/archived-job idle task routes to separate isolated Manager (and not the real Manager). Use only dedicated probe threads, tiny deterministic response instructions, gpt-5.6-terra/max where a model must be selected. This is authorized integration testing, no unregistered business tasks or real user thread mutations. Keep all fixture paths under install test temporary root; create managed fixture TASK-ID/workspace records and cleanup/archive only those resources. Never set production MAM_ROOT to fixture, never probe GPUs, never recursively invoke compatibility/install. Inject/skip only the *already performed* lightweight compatibility call inside fixture scheduler to avoid recursion, not actual scheduling/delivery logic.
Run this live test before stopping/replacing production service, after lightweight checks; on failure report nonzero, preserve bounded diagnostic evidence and stop own probe daemon/turns safely. No repeated retries that consume model calls without bound. Successful install may explicitly report lightweight capabilities and real delivery pass separately. No manual pre-created thread/extra operator step should be mandatory on every install. A missing live App Server is an explicit compatibility failure even for fresh project; fresh awaiting_manager refers to production manager binding, not permission to skip installation compatibility. Separate pure unit tests from actual live test so normal unittest does not spend tokens or contact real Codex.
Manager has authorized the small real probe turns, but coordinate first live execution after runtime integration so it can be inspected; developing fake tests and module now does not require approval. If current App Server forbids dedicated empty thread creation or robust cleanup, report exact observed API blocker rather than silently downgrading to handshake PASS.

## Manager adjudication: no compatibility bypass API
Do NOT add the proposed fixture compatibility mapping/keyword-only start_service argument or persist a PASS certificate. There is no recursion if liveprobe is called only from install.sh and require_compatible stays the lightweight API probe. Repeating lightweight checks in start_service and detached child costs no model calls and is acceptable. Use the unchanged two-argument public lifecycle API and real scheduler. The previous permission to skip an already performed lightweight check was optional if needed, not a requirement to build new bypass plumbing. Proceed without waiting for any new runtime API.

## Runtime integration available
Runtime delivered bf157209d6eb9ebdc0d07f5344ca175501941598. You may cherry-pick this exact commit into your installer worktree (disjoint files) to run combined tests and build liveprobe against real APIs; keep your owned modifications separate. Manager integration branch currently f114fd6 includes runtime+entry docs. First isolated live test may now be run by you under the authorized fixture constraints, with bounded evidence in your report; production activation remains Manager. Source runtime is in independent review, any runtime bug goes to Manager/runtime owner rather than you editing their module.

## Runtime review status
Core review3c97bd4f found three blockers now assigned back to runtime owner: paused->resumed eligibility, unknown-source dedup retention, typed JSON-RPC rejection vs transport failure. This does not block implementing/testing nominal isolated liveprobe against bf157209. Final integration/install waits for follow-up runtime commits; shared two-argument lifecycle and require_compatible APIs remain unchanged. Future AppServerRequestError will subclass AppServerEventError, so existing catch compatibility is preserved.

## Runtime fixes available for integration/review
Follow-up commit45614a60f63517eff3075381c697d370f09bdccc fixes the three accepted blockers and adds regression tests; source full suite109 passed. Manager integration branch codex/proactive-wake-integration now includes it. Installer: cherry-pick follow-up into your combined tree before final live acceptance (public lifecycle unchanged). Reviewer: resume your same worktree, incorporate45614a6 and independently recheck the three transitions; final whole-task verdict still waits for installer/liveprobe candidate. Original bf157209 already present, do not duplicate it.
