task_revision: 74ea3cdcbeaec6e61e3957b803dd6487a0ab2d1a
固定论文 commit: 03eff890a3817749d5e04413ab1e744914977335

完成与未完成：

- 已只读审视该固定 commit 的 `README.md`、`docs/EXPERIMENT_PLAN_20260910.zh-CN.md` 与 `docs/RELATED_WORK_POSITIONING_20260910.zh-CN.md`；未使用未提交 schema 草稿或旧论文正文。
- 已完成本项研究设计审视。未改论文、业务代码或实验配置；未创建环境/worktree，未启动程序或占用 GPU。

workspace、各库交付 commit：无。本任务是只读讨论，MAM 也未登记业务仓库。

裁定：可按固定版本推进已授权首批，无需因本审视暂停、增加 seed 或引入新框架。Q2 已把 full 表示、动作目标、H、读取行、phase mask/逐坐标归约、数据与增广、训练 seed、评测初始条件固定；它足以支撑一个边界明确的主结论。

最有价值的一句话研究问题：在相同 full 表示、动作目标、H=50、K=30 和第 K 个消费状态下，将 phase 的 H 行目标由 `phase(t+j+1)` 改为重复 `phase(t+30)`，是否会在两个任务和独立训练 seed 中系统性改变闭环成功率？

结果判读：令 Δ = repeated-endpoint 成功率 − per-frame 成功率。跨任务和训练 seed 同向的 Δ>0 支持重复终点在该协议下更好；同向的 Δ<0 支持逐帧目标更好。无稳定方向或区间覆盖 0 只说明当前 H/K/任务/数据下证据未区分两种目标，不是等价证明，也不能推出特定梯度机制或所有记忆系统的一般规律。

预算优先级：先完成既定 Q2 的两任务 × 两目标 × 三训练 seed（12 个模型及其 100ep 配对评测）的时间对齐/损失/恢复验收；其次复用其 per-frame checkpoint 做 F 的反馈消费矩阵。F0 只作探索。对主结论而言，B 的其余三任务、U、C 与 R 是补充或外推证据，不应挤占上述闭环证据链；这不要求改变已授权首批。

仍会改变结论或实验决策的问题（3 项）：

1. 时间索引的验收条件尚未写成可机器核对的不变量。计划 §6 T（L95–97）定义 `j=0…49`、目标 `t+j+1`，但同时写“第30行”；§6 F（L91）也用 row K，§8（L129–130）只要求核对。若实现把 row30 当零基 `j=30`，per-frame 消费的是 `t+31` 而 repeated endpoint 是 `t+30`，Q2 不再是同一下一查询需求，F 也会错位。最小修订：在配置/2-rollout smoke metadata 中固定并断言“执行 `a_t…a_{t+K-1}` 后消费 ordinal `r=K`（代码 `j=K-1`），其参考状态为 `s_{t+K}`”；K=20/30/50 都记录该四元组。无需新训练或评测。

2. Q1 的 F 只覆盖 per-frame full 目标，不能证明目标时刻与反馈规则的联合规律。Q2 只在 K30/row30 比较目标（§1 L14、§3 L45）；F 复用 B 的 full-per-frame checkpoint（§5 L75、L80，§6 L91）。因此 F 的交互可写成“per-frame 目标下的部署选行规律”，不能泛化为 repeated-endpoint 也相同。最小修订是把论文结论限于该条件；若必须主张联合选择，预先从非核心评测预算换出 6 个 run，对 T 的 6 个 endpoint checkpoint 补 K30/row20（row30 已有），不加训练且不按结果选择是否运行。这排除“F 模式仅属于 per-frame”的剩余解释。

3. Q3 的 K50 单元混入了训练—部署反馈节奏转移。serial 首批固定 previous lag30 并称其与 K30 相配（§4 L64），但 C 要以同一真实/c0 模型做 K30/K50 的差之差（§5 L78，§6 L107）。K50 的差异因而可以来自未在训练中经历的 50-step 反馈年龄，而非字段内容与匹配执行协议的交互。当前设计仍可报告为“K30 训练 serial 协议对 K50 的端到端鲁棒性”；若要作匹配的 content×K 主张，预先以 6 个训练名额（真实/c0 各三 seed）替换/延后 U，训练 K50 对齐的两臂或在两臂共同使用预定 K 混合训练。它排除的正是 cadence shift，不应根据中途成绩触发。

验证结果与成果位置：上述结论仅来自固定 commit 的只读 `git show`；无业务测试适用。成果为本报告。
