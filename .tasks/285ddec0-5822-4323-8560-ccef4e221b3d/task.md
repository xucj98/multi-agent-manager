# 九任务时间尺度事实审计：训练采样、状态监督与在线观测/更新频率

## 目标与最新用户约束

只读核实当前状态预测的时间接口，为 Manager 亲自设计跨九任务实证研究提供事实。用户明确要求考虑提高**状态预测频率**，保持 action chunk 执行长度、动作重规划频率不变；已有扫描提示过高动作闭环频率可能变差。不得把缩短 K 当成该提案，不设计新架构或裁决论文主张。

## 范围

1. 从冻结 OpenPI a869498f01a246752d7e5c6ed5ccd5dfdd9b3ff4 与 robot-bridge f9626636c4776d8eb15f9c556775cb2d12c000e5 核实：训练 query 行采样、J/T/S 标签时刻/mask、真实数据 FPS/转换降采样、在线观测获取/状态估计/状态缓存更新/动作查询/执行实际计时。每条结论给 commit、路径和行号；区分示范时间与仿真实际时间。
2. 核实目前 chunk 内逐行 J 输出是否是仅依赖 chunk 起点观测的未来预测，是否存在接收 chunk 内新观测的状态专用调用；S 能否只调用状态路径、是否依赖动作历史或 lag30 训练合同，当前接口哪些位置可容纳频繁状态估计而不改变动作调度。只报告现成能力/限制，不实施。
3. 从 RMBench 现有任务代码/说明审计九任务信息何时可见、需要保留什么、状态转移是否有短暂视觉/动作/接触事件。明确静态历史信息、事件更新、累积状态的差别；不要预设每个任务都需要高频。不用在线 simulator GT 作策略输入。需要新传感信息或语义字段时明确记录，不能称单频率变化。
4. 定位既有 K 扫描的可靠原始结果入口，用以记录约束；不重跑扫描或根据测试结果选频率。

## 执行与交付

先读 AGENTS.md、mam task show；必要独立只读 worktree 用 mam workspace add 创建，禁止改运行树。可直接读取固定 git 对象/已验收证据；不启动 GPU、rollout、正式训练、数据生成或传输。实质结果写 workspace/audit.md，发布 report，注明可验收稳定文件。预计短 CPU 分析无需 job。事实审计完成即可结束 turn，由 MAM 通知。其他已在跑训练不受影响。
