# 本集群的 agent 协作入口

本仓库管理当前集群的开发任务。本机与 `wuwen-1` 共享 `/mnt/public` 文件系统，同一路径下的代码、数据和环境两边都可见。具体操作见 [README.md](README.md)。

- 开始任务时明确目标、负责人、涉及的仓库和交付要求。进入某个库工作前，读取它的 `AGENTS.md` 和本次任务相关的规范。
- 讨论、只读调查和监控直接读取已有资料。修改代码和独立 review 使用自己的 worktree；需要运行项目代码时才安装环境。
- 每个 agent 使用 `Projects/workspace/<task-role-id>` 下自己的目录，只建立需要的库。同一任务继续使用已有 workspace；各库的创建方法见本库入口。
- 实验按照所属库的规范执行，分配时写清实验组、结果位置、GPU 和监控负责人。
- 所有新的工作日志统一写入本仓库 `.worklogs/`，每项任务维护一份简短记录。实验配置和结果继续保存在实验所属库。
- 交付时报告 commit、验证结果和成果位置，清理临时文件。Manager 接收成果后回收 workspace；仍有任务运行时，写清保留哪些目录、由谁继续负责。
- 修改清理功能时，测试它会保留未提交代码和仍在使用的工作区，而且不会删除软链接指向的数据。
